/** * @description Abstraksi untuk operasi data terkait tanda tangan dan dokumen * Kelas ini berfungsi sebagai kontrak (interface) yang harus diikuti oleh * semua implementasi repository (misalnya, PrismaSignatureRepository). */class SignatureRepository {    /**     * @description Mengambil dokumen dari database berdasarkan ID.     * @param {number|string} documentId - ID dokumen.     * @returns {Promise<object|null>} - Mengembalikan object dokumen atau null jika tidak ditemukan     */    async getDocumentById(documentId) {        throw new Error('Metode getDocumentById belum diimplementasikan.');    }    /**@description Mengambil detail tanda tangan dari database     * @param {number|string} documentId - ID dokumen     * @param {string} userId - ID pengguna     * @returns {Promise<obejct|null>} - Mengembalikan object tanda tangan atau null jika tidak ditemukan     */    async getSignatureDetails(documentId, userId) {        throw new Error('metode getSignatureDetails belum diimplementasikan.');    }}export default SignatureRepository;