/** * Wrapper untuk menangani error di dalam route handler async secara otomatis. * Menangkap error dan meneruskannya ke middleware error handler global Express. * @param {function} fn - Fungsi controller async (req, res, next). * @returns {function} - Fungsi yang akan dieksekusi oleh Express. */const asyncHandler = (fn) => (req, res, next) => {    Promise.resolve(fn(req, res, next)).catch(next);};export default asyncHandler;