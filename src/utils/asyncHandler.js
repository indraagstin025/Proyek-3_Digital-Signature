// src/utils/asyncHandler.js/** * @description Wrapper untuk menangani error pada fungsi async di Express. * Menghilangkan kebutuhan untuk blok try...catch di setiap controller. * @param {Function} fn - Fungsi controller async yang akan dieksekusi. * @returns {Function} Fungsi baru yang bisa digunakan sebagai middleware Express. */const asyncHandler = (fn) => (req, res, next) => {    Promise.resolve(fn(req, res, next)).catch(next);};export default asyncHandler;