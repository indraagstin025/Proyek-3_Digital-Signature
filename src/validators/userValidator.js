import { body, param } from 'express-validator';const updateUserValidator = [    body('name')        .optional()        .trim()        .notEmpty().withMessage('Nama tidak boleh kosong jika diisi.'),    body('phoneNumber')        .optional()        .trim()        .isMobilePhone('id-ID').withMessage('Format nomor telepon Indonesia tidak valid.'),    // 'title' sepertinya lebih cocok untuk jabatan/posisi, ini sudah benar.    body('title')        .optional()        .trim()        .notEmpty().withMessage('Jabatan tidak boleh kosong jika diisi.'),    body('address')        .optional()        .trim()        .notEmpty().withMessage('Alamat tidak boleh kosong jika diisi')];// Validasi untuk parameter 'pictureId' di URLconst pictureIdValidator = [    param('pictureId').isUUID().withMessage('Format ID foto profil tidak valid.')];export const userValidation = {    update: updateUserValidator,    pictureId: pictureIdValidator, // <-- Tambahkan ini};