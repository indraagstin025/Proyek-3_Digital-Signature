import express from 'express';import authMiddleware from '../middleware/authMiddleware.js';import { checkAdminRole } from '../middleware/roleMiddleware.js';import { userValidation } from "../validators/userValidator.js";import { validate } from "../middleware/validate.js";import { uploadImage } from "../middleware/uploadMiddleware.js";export default (userController, adminController) => {    const router = express.Router();    /**     * @route   GET /api/users/me     * @desc    Ambil profil user yang sedang login     * @access  Private     */    router.get('/me', authMiddleware, userController.getMyProfile);    /**     * @route   PUT /api/users/me     * @desc    Perbarui profil user yang sedang login     * @access  Private     */    router.put(        '/me',        authMiddleware,           // Pastikan user sudah login        uploadImage.single("profilePicture"),        userValidation.update,    // Terapkan aturan validasi untuk update        validate,                 // Cek hasil validasi, kirim error jika tidak valid        userController.updateMyProfile // Jalankan controller jika data valid    );    /**     * @route   GET /api/users/all-users     * @desc    Ambil semua user (hanya admin)     * @access  Private (Admin Only)     */    router.get('/all-users', authMiddleware, checkAdminRole, adminController.getAllUsers);    return router;};