// File: routes/historyRoutes.jsimport express from "express";import authMiddleware from "../middleware/authMiddleware.js";/** * Membuat router untuk endpoint terkait History. * @param {object} historyController */export const createHistoryRoutes = (historyController) => {    const router = express.Router();    /**     * @route   GET /api/history     * @desc    Mengambil daftar riwayat tanda tangan user.     * @access  Private     */    router.get("/", authMiddleware, historyController.getMyHistory);    return router;};