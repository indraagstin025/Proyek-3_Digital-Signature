import express from 'express';import authMiddleware from "../middleware/authMiddleware.js";export default (signatureControlller) => {    const router = express.Router();    /**     * Tanda tangan dokumen.     * @route POST /api/signatures/sign/:id     * Akses: Private     */    router.post('/sign/:id', authMiddleware, signatureControlller.handleSignDocument);    /**     * Verifikasi tanda tangan berbasis QR Code.     * @route GET /api/signatures/verify/:id     * Akses: Public     */    // routes/signatureRoutes.js    router.get('/verify', signatureControlller.handleVerifySignature);    return router;};