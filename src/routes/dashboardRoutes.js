import express from "express";import authMiddleware from "../middleware/authMiddleware.js";/** * @description Factory function untuk rute Dashboard. * @param {Object} dashboardController - Instance controller yang sudah di-inject service. * @returns {import("express").Router} Router Express. */export default (dashboardController) => {    const router = express.Router();    /**     * Rute Dashboard Privat (Memerlukan Token)     * Alur: Cek Token -> Controller     */    router.get("/", authMiddleware, dashboardController.getSummary);    return router;};