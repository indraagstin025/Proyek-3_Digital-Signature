import express from "express";import { verifyToken } from "../middlewares/authMiddleware.js"; // Sesuaikan path middleware authimport { createDashboardController } from "../controllers/dashboardController.js";import { dashboardService } from "../services/dashboardService.js";const router = express.Router();// Inisialisasi Controller dengan Service yang sudah dibuatconst dashboardController = createDashboardController(dashboardService);// Definisi Routerouter.get("/", verifyToken, dashboardController.getSummary);export default router;