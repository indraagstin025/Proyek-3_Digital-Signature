class BaseError extends Error {    /**     * @param {string} code - Kode error unik (misalnya: AUTH_INVALID, DOC_NOT_FOUND)     * @param {number} statusCode - HTTP status code (contoh: 400, 401, 404, 500).     * @param {string} message - Pesan error yang jelas.     */    constructor(code, statusCode, message) {        super(message); // Memanggil constructor dari kelas Error bawaan        this.code = code;        this.statusCode = statusCode;        this.status = `${statusCode}`.startsWith("4") ? "fail" : "error";        this.isOperational = true; // Menandakan ini error yang disengaja        // Baris ini yang perlu diperbaiki        Error.captureStackTrace(this, this.constructor);    }}export default BaseError;