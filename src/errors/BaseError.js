class BaseError extends error {    /**     * @param {string} code - Kode error unik (misalnya: AUTH_INVALID, DOC_ENCRYPTED)     * @param {number} statusCode - HTTP status code (contoh: 400, 401, 404, 500).     * @param {string} message - Pesan error yang jelas untuk user dan pembuat.     */    constructor(code, statusCode, message) {        super(message);        this.code = code;        this.statusCode = statusCode;        this.status = `${statusCode}`.startsWith("4") ? "fail" : "error";        Error.captureStackTrace(this. this.constructor);    }}export default BaseError;