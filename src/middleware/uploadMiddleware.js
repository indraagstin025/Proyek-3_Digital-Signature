import multer from 'multer';import CommonError from '../errors/CommonError.js'; // <-- Impor CommonErrorconst storage = multer.memoryStorage();function documentFileFilter(req, file, cb) {    if (file.mimetype === 'application/pdf' ||        file.mimetype === 'application/msword' ||        file.mimetype === 'application/vnd.openxmlformats-officedocument.wordprocessingml.document') {        cb(null, true);    } else {        // Gunakan error kustom kita        cb(CommonError.InvalidFileType('Tipe file tidak diizinkan! Hanya PDF dan Word yang didukung.'), false);    }}function imageFileFilter(req, file, cb) {    if (file.mimetype.startsWith('image/')) {        cb(null, true);    } else {        // Gunakan error kustom kita        cb(CommonError.InvalidFileType('Tipe file tidak diizinkan! Hanya file gambar yang didukung.'), false);    }}export const uploadDocument = multer({    storage: storage,    limits: {        fileSize: 5 * 1024 * 1024, // 5MB    },    fileFilter: documentFileFilter,});export const uploadImage = multer({    storage: storage,    limits: {        fileSize: 2 * 1024 * 1024, // 2MB untuk gambar    },    fileFilter: imageFileFilter,});