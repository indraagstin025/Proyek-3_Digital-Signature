import multer from 'multer';const storage = multer.memoryStorage();function documentFileFilter(req, file, cb) {    if (file.mimetype === 'application/pdf' ||        file.mimetype === 'application/msword' ||        file.mimetype === 'application/vnd.openxmlformats-officedocument.wordprocessingml.document') {        cb(null, true);    } else {        cb(new Error('Tipe file tidak diizinkan! Hanya PDF dan Word.'), false);    }}function pdfFileFilter(req, file, cb) {    if (file.mimetype === 'application/pdf') {        cb(null, true);    } else {        cb(new Error('Tipe file tidak diizinkan! Hanya PDF untuk diverifikasi.'), false);    }}function imageFileFilter(req, file, cb) {    if (file.mimetype.startsWith('image/')) {        cb(null, true);    } else {        cb(new Error('Tipe file tidak diizinkan! Hanya gambar.'), false);    }}export const uploadDocument = multer({    storage: storage,    limits: {        fileSize: 5 * 1024 * 1024,    },    fileFilter: documentFileFilter,});export const uploadImage = multer({    storage: storage,    limits: {        fileSize: 5 * 1024 * 1024,    },    fileFilter: imageFileFilter,});export const uploadPdfForVerification = multer({    storage: storage,    limits: {      fileSize: 5 * 1024 * 1024,    },    fileFilter: pdfFileFilter,});